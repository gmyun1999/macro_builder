# File Condition: {{ block.condition_type }}
def check_{{ block.condition_type.lower() }}():
    value = "{{ block.value }}"
    detail_condition_types = {{ block.detail_condition_type | tojson }}
    # 조건을 확인하는 로직을 여기에 추가
    print("조건 확인: ", "{{ block.condition_type }}", "값: ", value)
    # 실제 로직을 구현해야 합니다.
    return True  # 예시로 항상 True 반환
